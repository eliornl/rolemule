/**
 * Phase 1 stub test — ensures Vitest is wired. Real shared-module tests land in Phase 2.
 */
import { describe, it, expect } from 'vitest';

describe('frontend toolchain', () => {
  it('runs vitest', () => {
    expect(true).toBe(true);
  });
});
