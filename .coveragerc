[run]
omit =
    venv/*
    tests/*
    alembic/*
    _private/*
    cli/*
    applypilot_client/*

[report]
# Multi-provider LLM surface (providers, availability, BYOK gates) expanded
# statement count; keep gate at 96 until tools/CV optimizer paths are fully attributed.
fail_under = 96
